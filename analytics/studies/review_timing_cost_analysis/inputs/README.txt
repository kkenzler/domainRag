review_timing_cost_analysis inputs
=================================

Purpose
- This folder can hold small copied reference files or hand-curated source manifests used by the timing/cost study.
- Prefer keeping it light. Do not bulk-copy large analytics artifacts here unless a local frozen snapshot is actually needed.

Current approach
- The study currently points at upstream canonical analytics files outside this root rather than copying them.
- If a snapshot is later needed for reproducibility, copy only the minimal supporting file set and record why.
